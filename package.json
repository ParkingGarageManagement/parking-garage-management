{
  "name": "parking-garage-management-system",
  "version": "1.0.0",
  "description": "Full stack parking garage management system",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "install-deps": "concurrently \"cd backend && npm install\" \"cd frontend && npm install\"",
    "dev": "npm run install-deps && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}